{
  "name": "Offline Partitions Count",
  "description": "Sum of kafka_controller_kafkacontroller_value{name=OfflinePartitionsCount} across controllers.",
  "programText": "data('kafka_controller_kafkacontroller_value', filter=filter('namespace', 'confluent') and filter('service.name', 'kafka-controller') and filter('name', 'OfflinePartitionsCount')).sum().publish(label='Offline Partitions Count')\n",
  "options": {
    "type": "SingleValue",
    "colorBy": "Scale",
    "unitPrefix": "Metric",
    "colorScale2": [
      {
        "lte": 0,
        "paletteIndex": 18
      },
      {
        "gt": 0,
        "paletteIndex": 16
      }
    ]
  }
}
