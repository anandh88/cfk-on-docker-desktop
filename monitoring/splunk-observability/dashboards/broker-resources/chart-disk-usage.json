{
  "name": "Disk Usage %",
  "description": "kafka_log_log_value{name=Size} / kube_persistentvolumeclaim_resource_requests_storage_bytes, as a percentage.",
  "programText": "a = data('kafka_log_log_value', filter=filter('namespace', 'confluent') and filter('service.name', 'kafka-broker') and filter('name', 'Size') and filter('service.instance.id', 'kafka-0')).sum()\nb = data('kube_persistentvolumeclaim_resource_requests_storage_bytes', filter=filter('namespace', 'confluent') and filter('persistentvolumeclaim', 'data0-kafka-0')).sum()\n(a/b*100).publish(label='Disk Usage %')\n",
  "options": {
    "type": "SingleValue",
    "colorBy": "Scale",
    "unitPrefix": "Metric",
    "secondaryVisualization": "Radial",
    "colorScale2": [
      {
        "gte": 0,
        "lt": 70,
        "paletteIndex": 18
      },
      {
        "gte": 70,
        "lt": 90,
        "paletteIndex": 6
      },
      {
        "gte": 90,
        "lte": 100,
        "paletteIndex": 16
      }
    ]
  }
}
