{
  "name": "Source Record Poll Rate",
  "description": "kafka_connect_source_task_metrics_source_record_poll_rate, by connector+task. Currently no live data - zero connectors deployed on this cluster.",
  "programText": "data('kafka_connect_source_task_metrics_source_record_poll_rate', filter=filter('namespace', 'confluent') and filter('service.name', 'kafka-connect') and filter('task', '*')).sum(by=['connector','task']).publish(label='Source Record Poll Rate')\n",
  "options": {
    "type": "TimeSeriesChart",
    "defaultPlotType": "LineChart",
    "colorBy": "Dimension",
    "stacked": false,
    "includeZero": false,
    "axisPrecision": 3,
    "unitPrefix": "Metric",
    "lineChartOptions": {
      "showDataMarkers": false
    },
    "onChartLegendOptions": {
      "showLegend": true,
      "dimensionInLegend": "plot_label"
    },
    "programOptions": {
      "disableSampling": false,
      "timezone": "UTC"
    },
    "axes": [
      null,
      null
    ]
  }
}
