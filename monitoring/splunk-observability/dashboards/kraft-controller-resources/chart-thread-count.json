{
  "name": "Thread Count",
  "description": "Raw jvm_threads_current.",
  "programText": "data('jvm_threads_current', filter=filter('namespace', 'confluent') and filter('service.name', 'kafka-controller') and filter('service.instance.id', 'kraftcontroller-0')).publish(label='Thread Count')\n",
  "options": {
    "type": "SingleValue",
    "colorBy": "Dimension",
    "secondaryVisualization": "None",
    "unitPrefix": "Metric"
  }
}
