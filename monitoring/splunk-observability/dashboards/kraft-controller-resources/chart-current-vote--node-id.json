{
  "name": "Current Vote (node id)",
  "description": "Raw kafka_server_raft_metrics_current_vote.",
  "programText": "data('kafka_server_raft_metrics_current_vote', filter=filter('namespace', 'confluent') and filter('service.name', 'kafka-controller') and filter('service.instance.id', 'kraftcontroller-0')).publish(label='Current Vote')\n",
  "options": {
    "type": "SingleValue",
    "colorBy": "Dimension",
    "secondaryVisualization": "None",
    "unitPrefix": "Metric"
  }
}
